# Copy this file to .env.prod and replace all placeholder values.
# Use strong random values for passwords.
# All production compose commands must include: --env-file .env.prod

# Optional image tag for both web and api images.
IMAGE_TAG=latest

# Bind the web container port:
# - 127.0.0.1 (recommended when using host-level nginx reverse proxy)
# - 0.0.0.0 (direct public access to port 4173)
WEB_BIND_ADDRESS=127.0.0.1

# MariaDB / API database settings
DB_NAME=pokemon_collector
DB_USER=pokemon_app
DB_PASSWORD=replace-with-strong-db-password
MARIADB_ROOT_PASSWORD=replace-with-strong-root-password

# Seeded admin user for the local API
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-strong-admin-password
