# Copy to .env.proxy and set real values before running Traefik.

# Domain this stack should serve.
APP_DOMAIN=app.example.com

# Email used by Let's Encrypt registration and expiry notices.
TRAEFIK_ACME_EMAIL=you@example.com

# Docker network Traefik should use to reach the web container.
# If you use the commands in README with -p pokemoncollector, keep this value.
TRAEFIK_DOCKER_NETWORK=pokemoncollector_proxy
